#!/usr/bin/env node
/**
 * @deprecated U5 — unnamed rules は DSL stub / registry が正。本スクリプトは無効。
 * ルール付与は `emit-core-dsl-stubs` と pipeline extract を使用すること。
 */
console.warn(
  "annotate-unnamed-rules.mjs is deprecated (U5). Edit generated DSL stubs or pipeline instead.",
);
process.exit(0);
